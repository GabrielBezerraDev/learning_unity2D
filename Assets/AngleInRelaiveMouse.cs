using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AngleInRelativeMouse : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void testAnglebetween(float currentAngle, float x, float y)
    {
        if (currentAngle >= x || currentAngle <= y)
        {

        }
    }

    public void testSingleAngle(float x)
    {

    }
}
